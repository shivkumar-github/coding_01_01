/*
(a mod m)+(b mod m)  mod m = (a+b)  mod m
(a mod m)−(b mod m)  mod m = (a−b)  mod m
(a mod m)⋅(b mod m)  mod m = (a⋅b) mod m -->
*/

#include <iostream>
using namespace std;
int modularExponentiation(int x, int n, int m)
{
    if (n % 2)
    {
    }
}
int main()
{

    // cout << modularExponentiation( ) << endl;
    return 0;
}